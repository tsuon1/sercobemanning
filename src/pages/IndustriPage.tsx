import ServicePageLayout from "@/components/ServicePageLayout";
import heroImg from "@/assets/hero-industri.jpg";

const IndustriPage = () => (
  <ServicePageLayout heroImage={heroImg} pageKey="industriPage" />
);

export default IndustriPage;